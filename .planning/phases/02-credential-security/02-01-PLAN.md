---
phase: 02-credential-security
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pom.xml
  - RELEASE.md
  - src/test/java/org/liuzx/jce/provider/SigningCredentialConfigTest.java
autonomous: true
requirements: [SEC-01]

must_haves:
  truths:
    - "pom.xml 不再出现明文签名口令（storepass/keypass 的值不得是字面量）"
    - "签名凭据从 ~/.m2/settings.xml 的 jce-signing profile 注入"
    - "缺少凭据时构建失败信息明确指向所需属性"
  artifacts:
    - path: "pom.xml"
      provides: "属性化签名配置"
      contains: "${jce.storepass}"
    - path: "RELEASE.md"
      provides: "jce-signing profile 配置说明"
      contains: "jce-signing"
    - path: "src/test/java/org/liuzx/jce/provider/SigningCredentialConfigTest.java"
      provides: "pom 无明文口令的静态断言"
      contains: "storepass"
  key_links:
    - from: "pom.xml"
      to: "~/.m2/settings.xml"
      via: "jce-signing profile 属性"
      pattern: "jce.storepass"
---

<objective>
把 JCE 签名凭据（keystore 口令、别名、TSA）从 `pom.xml` 明文迁移到 `settings.xml` 注入的属性，并文档化配置方式。

Purpose: 消除公开弱口令泄露面（CONCERNS C1），使发布路径可审计（Phase 2 目标）。
Output: 参数化的 `pom.xml`、`RELEASE.md` 配置章节、静态断言测试。
</objective>

<execution_context>
@$HOME/.Codex/get-shit-done/workflows/execute-plan.md
@$HOME/.Codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-credential-security/02-RESEARCH.md
@.planning/phases/02-credential-security/02-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 参数化主 JAR 签名凭据</name>
  <files>pom.xml</files>
  <read_first>pom.xml, RELEASE.md</read_first>
  <action>在 `<properties>` 中新增非敏感默认：`<jce.keystore>${project.basedir}/keystore.jks</jce.keystore>`、`<jce.keystore.alias>dayou</jce.keystore.alias>`、`<jce.tsa>http://timestamp.sectigo.com</jce.tsa>`。把 `maven-jarsigner-plugin` 的 `<keystore>` 改为 `${jce.keystore}`、`<alias>` 改为 `${jce.keystore.alias}`、`<storepass>` 改为 `${jce.storepass}`、`<keypass>` 改为 `${jce.keypass}`、`<tsa>` 改为 `${jce.tsa}`。`${jce.storepass}` / `${jce.keypass}` 只允许来自 `settings.xml` 的 `jce-signing` profile 或 `-D` 覆盖，不得在 pom 内提供默认值。若属性未设置，jarsigner 会以空口令失败——在 Task 3 的文档中给出明确修复指引。</action>
  <verify>grep -n 'storepass' pom.xml</verify>
  <acceptance_criteria>
    - pom.xml 中 `maven-jarsigner-plugin` 的 `<storepass>` 值为 `${jce.storepass}`，`<keypass>` 为 `${jce.keypass}`
    - pom.xml 中 `maven-jarsigner-plugin` 的 `<keystore>` 值为 `${jce.keystore}`
    - pom.xml 不再出现 `<storepass>123456</storepass>` 或 `<keypass>123456</keypass>`
  </acceptance_criteria>
  <done>主 JAR 签名凭据全部属性化</done>
</task>

<task type="auto">
  <name>Task 2: 参数化依赖 JAR 签名凭据</name>
  <files>pom.xml</files>
  <read_first>pom.xml</read_first>
  <action>把 `maven-antrun-plugin` 的 `sign-libs` 执行中两个 `<signjar>` 的 `alias="dayou"` 改为 `alias="${jce.keystore.alias}"`、`storepass="123456"` 改为 `storepass="${jce.storepass}"`、`keypass="123456"` 改为 `keypass="${jce.keypass}"`、`keystore="${project.basedir}/keystore.jks"` 改为 `keystore="${jce.keystore}"`、`tsaurl="http://timestamp.sectigo.com"` 改为 `tsaurl="${jce.tsa}"`。保持 jar 文件名不变（依赖签名与版本解耦属 BUILD-02/Phase 3）。</action>
  <verify>grep -nE 'signjar|storepass|tsaurl' pom.xml | head -20</verify>
  <acceptance_criteria>
    - antrun 的两个 `<signjar>` 均不含字面量 `123456` 或 `dayou`
    - antrun 的 `keystore` 与 `tsaurl` 分别引用 `${jce.keystore}` 与 `${jce.tsa}`
    - `grep -E '>123456<|storepass="123456"' pom.xml` 无输出
  </acceptance_criteria>
  <done>依赖 JAR 签名凭据全部属性化</done>
</task>

<task type="auto">
  <name>Task 3: 文档化 jce-signing profile 并加静态断言</name>
  <files>RELEASE.md, src/test/java/org/liuzx/jce/provider/SigningCredentialConfigTest.java</files>
  <read_first>RELEASE.md, pom.xml, src/test/java/org/liuzx/jce/provider/SDFConfigTest.java</read_first>
  <action>在 `RELEASE.md` 新增「JCE 签名凭据（jce-signing）」小节：给出 `~/.m2/settings.xml` 中常驻 profile 示例（`<jce.storepass>`、`<jce.keypass>`，可选 `<jce.tsa>`），说明可选用 `-D` 覆盖（CI），并给出缺凭据时的症状与修复（jarsigner 空口令失败 → 配置 profile）。新增 `SigningCredentialConfigTest`：读取项目根 `pom.xml` 文本（通过 `Paths.get("pom.xml")`，测试工作目录为项目根），断言不含正则 `storepass>123456<`、`keypass>123456<`、`storepass="123456"`、`keypass="123456"`，且包含 `${jce.storepass}` 与 `${jce.keypass}`。</action>
  <verify>mvn -o test -DskipTests=false -Dtest=SigningCredentialConfigTest</verify>
  <acceptance_criteria>
    - `RELEASE.md` 含 `jce-signing` 与 `jce.storepass`
    - `SigningCredentialConfigTest` 通过
    - 测试在无硬件、无 settings 凭据时也能运行
  </acceptance_criteria>
  <done>配置文档与静态门禁就绪</done>
</task>

</tasks>

<verification>
- [ ] `grep -nE 'storepass|keypass' pom.xml` 只出现 `${jce.storepass}` / `${jce.keypass}`
- [ ] `mvn -o test -DskipTests=false -Dtest=SigningCredentialConfigTest` 通过
</verification>

<success_criteria>
- SEC-01 达标：pom 无明文签名口令，凭据来自 settings.xml
- 文档给出可直接复制的配置
</success_criteria>

<output>
After completion, create `.planning/phases/02-credential-security/02-01-SUMMARY.md`
</output>
