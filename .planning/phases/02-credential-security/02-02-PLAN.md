---
phase: 02-credential-security
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/java/org/liuzx/jce/demo/SdfSmokeTest.java
  - src/main/java/org/liuzx/jce/demo/Main.java
  - scripts/sdf-smoke.sh
  - scripts/verify-no-secret-in-argv.sh
  - README.md
autonomous: true
requirements: [SEC-03]

must_haves:
  truths:
    - "冒烟测试的 PIN 经环境变量传入，不出现在 java 进程命令行中"
    - "演示程序压力测试不再从位置参数读取 PIN"
    - "PIN 仍按需申请、finally 释放，且不写入日志"
  artifacts:
    - path: "src/main/java/org/liuzx/jce/demo/SdfSmokeTest.java"
      provides: "从环境变量读取 PIN"
      contains: "LIUZX_SMOKE_PIN"
    - path: "scripts/sdf-smoke.sh"
      provides: "export 而非 -D 传递 PIN"
      contains: "export LIUZX_SMOKE_PIN"
    - path: "scripts/verify-no-secret-in-argv.sh"
      provides: "argv 不含 PIN 的断言脚本"
      contains: "ps"
  key_links:
    - from: "scripts/sdf-smoke.sh"
      to: "src/main/java/org/liuzx/jce/demo/SdfSmokeTest.java"
      via: "LIUZX_SMOKE_PIN 环境变量"
      pattern: "LIUZX_SMOKE_PIN"
---

<objective>
把测试与演示程序的 PIN 从命令行 `-D` 与位置参数迁移到环境变量/交互输入，消除 `ps` 可见的凭据暴露面。

Purpose: 消除 CONCERNS C3，落实「PIN 不进 argv」。
Output: 改造后的冒烟脚本与演示程序、argv 断言脚本、README 安全说明更新。
</objective>

<execution_context>
@$HOME/.Codex/get-shit-done/workflows/execute-plan.md
@$HOME/.Codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-credential-security/02-RESEARCH.md
@.planning/phases/02-credential-security/02-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: SdfSmokeTest 改为从环境变量读取 PIN</name>
  <files>src/main/java/org/liuzx/jce/demo/SdfSmokeTest.java</files>
  <read_first>src/main/java/org/liuzx/jce/demo/SdfSmokeTest.java, scripts/sdf-smoke.sh</read_first>
  <action>新增 `private static char[] smokePin()`：优先读取环境变量 `LIUZX_SMOKE_PIN`；若仍设置了旧系统属性 `liuzx.sdf.smoke.pin`，则打印一行告警「liuzx.sdf.smoke.pin is deprecated and visible in ps; use LIUZX_SMOKE_PIN」并**忽略**该属性（不再读取其值）。用 `smokePin()` 替换 `runInternalChecks` 与 `runFacadeChecks` 中所有 `System.getProperty("liuzx.sdf.smoke.pin")`。同一 PIN 只读取一次，返回 `char[]`，使用后不缓存到静态字段。其余 `liuzx.sdf.smoke.*` 索引属性保持不变（非敏感）。</action>
  <verify>grep -n "LIUZX_SMOKE_PIN\|liuzx.sdf.smoke.pin" src/main/java/org/liuzx/jce/demo/SdfSmokeTest.java</verify>
  <acceptance_criteria>
    - `SdfSmokeTest` 含 `LIUZX_SMOKE_PIN` 环境变量读取
    - `SdfSmokeTest` 不再从 `System.getProperty("liuzx.sdf.smoke.pin")` 读取口令（仅出现告警字符串）
    - 无静态 `char[]` 字段缓存 PIN
  </acceptance_criteria>
  <done>冒烟 PIN 入口迁移完成</done>
</task>

<task type="auto">
  <name>Task 2: sdf-smoke.sh 用 export 传递 PIN</name>
  <files>scripts/sdf-smoke.sh, scripts/verify-no-secret-in-argv.sh</files>
  <read_first>scripts/sdf-smoke.sh</read_first>
  <action>删除 `JAVA_OPTS+=("-Dliuzx.sdf.smoke.pin=${SMOKE_PIN}")` 一行；改为在调用 `java` 前 `export LIUZX_SMOKE_PIN="${SMOKE_PIN}"`（`SMOKE_PIN` 为空时不 export）。保留 `SMOKE_BAD_PIN` 的处理：同样改为环境变量 `LIUZX_SMOKE_BAD_PIN` 并在 `SdfSmokeTest` 读取（替换 `liuzx.sdf.smoke.badPin`）。新增 `scripts/verify-no-secret-in-argv.sh`：以 `SMOKE_SKIP_BUILD=1` 启动冒烟（`LIUZX_SMOKE_PIN` 设为哨兵值 `SENTINEL_PIN_XYZ`），在运行期用 `pgrep -f SdfSmokeTest` 找到 pid 并断言 `ps -o command= -p <pid>` 不含哨兵值；脚本退出码 0 表示无泄漏。</action>
  <verify>bash -n scripts/sdf-smoke.sh && bash -n scripts/verify-no-secret-in-argv.sh</verify>
  <acceptance_criteria>
    - `sdf-smoke.sh` 不再包含 `-Dliuzx.sdf.smoke.pin`
    - `sdf-smoke.sh` 含 `export LIUZX_SMOKE_PIN`
    - 两个脚本 `bash -n` 退出码 0
    - `verify-no-secret-in-argv.sh` 在无设备时也能完成 argv 断言（进程存在即断言）
  </acceptance_criteria>
  <done>脚本侧 PIN 不再进入 argv</done>
</task>

<task type="auto">
  <name>Task 3: 移除演示程序的位置参数 PIN 并更新文档</name>
  <files>src/main/java/org/liuzx/jce/demo/Main.java, README.md</files>
  <read_first>src/main/java/org/liuzx/jce/demo/Main.java, run.sh, README.md</read_first>
  <action>在 `Main.main` 中移除 `args[4].toCharArray()` 的位置参数口令读取（压力测试子命令）；改为：若需要口令则交互读取（`I18n.get("prompt.password")` + `scanner.nextLine()`），或从环境变量 `LIUZX_STRESS_PIN` 读取。保留线程数/时长/索引的位置参数。更新 `README.md` 压力测试用法：`./run.sh stress <线程数> <时长> <密钥索引>`，并说明 PIN 通过交互输入或 `LIUZX_STRESS_PIN`，以及「PIN 不再作为命令行参数」的安全提示。</action>
  <verify>grep -n "args\[4\]\|LIUZX_STRESS_PIN" src/main/java/org/liuzx/jce/demo/Main.java; grep -n "stress" README.md | head</verify>
  <acceptance_criteria>
    - `Main.java` 不再出现 `args[4]`
    - `Main.java` 含 `LIUZX_STRESS_PIN` 或交互读取
    - `README.md` 压力测试用法不再要求第 4 个位置参数为 PIN
  </acceptance_criteria>
  <done>演示程序 PIN 入口迁移完成</done>
</task>

</tasks>

<verification>
- [ ] `grep -rn "smoke.pin" scripts/ src/` 仅剩弃用告警字符串
- [ ] `mvn -o -q compile` 成功
- [ ] `bash -n scripts/verify-no-secret-in-argv.sh` 成功
</verification>

<success_criteria>
- SEC-03 达标：PIN 不经 `-D` 或位置参数
- 有可自动执行的 argv 泄漏断言
</success_criteria>

<output>
After completion, create `.planning/phases/02-credential-security/02-02-SUMMARY.md`
</output>
