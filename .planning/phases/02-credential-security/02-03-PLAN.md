---
phase: 02-credential-security
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/pack-smoke.sh
  - README.md
autonomous: true
requirements: [SEC-04]

must_haves:
  truths:
    - "打包默认不包含真实设备凭据配置文件（*.ini）"
    - "只有显式 PACK_INCLUDE_CONF=1 才复制真实配置，并打印醒目警告"
    - "默认产物仍可运行（含脱敏模板与 .example）"
  artifacts:
    - path: "scripts/pack-smoke.sh"
      provides: "默认脱敏 + 显式开关"
      contains: "PACK_INCLUDE_CONF"
  key_links:
    - from: "scripts/pack-smoke.sh"
      to: "conf/"
      via: "默认仅复制 *.ini.example 并生成模板"
      pattern: "ini.example"
---

<objective>
让冒烟测试打包默认不携带设备凭据配置，改为脱敏模板 + 显式开关。

Purpose: 消除 CONCERNS C3 的「打包默认泄露设备配置」风险。
Output: 改造后的 `pack-smoke.sh` 与 README 说明。
</objective>

<execution_context>
@$HOME/.Codex/get-shit-done/workflows/execute-plan.md
@$HOME/.Codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-credential-security/02-RESEARCH.md
@.planning/phases/02-credential-security/02-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 默认只打包脱敏配置模板</name>
  <files>scripts/pack-smoke.sh</files>
  <read_first>scripts/pack-smoke.sh, HSM/README.md</read_first>
  <action>把当前「复制 CONF_SOURCE 到 conf/」的分支改为：当 `PACK_INCLUDE_CONF` 未设为 `1` 时，不复制真实 `*.ini`；改为若源目录存在 `*.ini.example` 则复制这些 example 文件，并为该 vendor 生成一个脱敏模板 `conf/<vendor>.ini`（内容用 `<HOST>`/`<PORT>` 占位，不含真实 IP/端口），同时在脚本输出中打印一行 `[pack] conf sanitized: real device config NOT included (set PACK_INCLUDE_CONF=1 to include it)`. 模板里不得回显源文件的真实主机值。保留 `CONF_HINT` 用于生成的 `run-smoke.sh` 提示文案。</action>
  <verify>bash -n scripts/pack-smoke.sh</verify>
  <acceptance_criteria>
    - `pack-smoke.sh` 含 `PACK_INCLUDE_CONF`
    - 默认分支不含 `cp "${CONF_SOURCE}"`（真实文件复制）
    - 默认分支生成 `/conf/` 下的 `.example` 或占位模板，且不含源文件真实 IP（模板用 `<HOST>` 占位）
    - `bash -n scripts/pack-smoke.sh` 退出码 0
  </acceptance_criteria>
  <done>默认打包脱敏</done>
</task>

<task type="auto">
  <name>Task 2: 增加显式开关与醒目告警</name>
  <files>scripts/pack-smoke.sh</files>
  <read_first>scripts/pack-smoke.sh</read_first>
  <action>新增环境变量 `PACK_INCLUDE_CONF`（默认 `0`）。当其等于 `1` 时，保持原有「复制真实文件/目录到 conf/」的行为，并在打包前打印多行警告：`[pack] WARNING: including real device config (may contain credentials/IPs); do not distribute this bundle`。更新脚本顶部用法注释，列出 `PACK_INCLUDE_CONF` 及其安全含义。</action>
  <verify>bash -n scripts/pack-smoke.sh</verify>
  <acceptance_criteria>
    - `PACK_INCLUDE_CONF=1` 路径存在且会复制真实配置
    - 该路径打印 `WARNING: including real device config`
    - 头部用法注释含 `PACK_INCLUDE_CONF`
  </acceptance_criteria>
  <done>显式开关与告警就绪</done>
</task>

<task type="auto">
  <name>Task 3: 更新 README 打包与安全说明</name>
  <files>README.md</files>
  <read_first>README.md</read_first>
  <action>更新 README「打包到密码机主机运行」小节：说明默认产物不含真实设备配置（仅 example/占位模板），需在目标主机填写 IP/端口或使用 `PACK_INCLUDE_CONF=1` 打包真实配置并自行承担分发风险；把原「conf/ 可能含设备凭据」提示替换为与脚本一致的措辞。同时在「非交互冒烟测试」小节把 PIN 传递改为环境变量 `LIUZX_SMOKE_PIN`（与 02-02 一致）。</action>
  <verify>grep -n "PACK_INCLUDE_CONF\|LIUZX_SMOKE_PIN" README.md</verify>
  <acceptance_criteria>
    - README 含 `PACK_INCLUDE_CONF`
    - README 含 `LIUZX_SMOKE_PIN`
    - README 不再以 `SMOKE_PIN` 作为 `-D` 示例
  </acceptance_criteria>
  <done>文档与脚本一致</done>
</task>

</tasks>

<verification>
- [ ] `bash -n scripts/pack-smoke.sh` 成功
- [ ] 默认打包产物 `tar -tzf` 不含真实 `conf/*.ini`（仅 `.example`/占位模板）
</verification>

<success_criteria>
- SEC-04 达标：默认不携带设备凭据配置，显式开关 + 告警
</success_criteria>

<output>
After completion, create `.planning/phases/02-credential-security/02-03-SUMMARY.md`
</output>
