---
phase: 02-credential-security
plan: 04
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - doc/SIGNING-KEY-ROTATION.md
  - RELEASE.md
  - keystore.jks
autonomous: false
requirements: [SEC-02]
user_setup:
  - service: jce-signing-keystore
    why: "生成新的 JCE 签名密钥库需要人工设定强口令，且密钥库与口令都不得进入版本库"
    env_vars:
      - name: jce.storepass
        source: "人工设定的强口令，写入 ~/.m2/settings.xml 的 jce-signing profile"
      - name: jce.keypass
        source: "同 storepass 或独立强口令"
    local_dev:
      - "keytool -genkeypair -alias dayou ... -keystore keystore.jks（口令用强口令）"

must_haves:
  truths:
    - "keystore.jks 使用强口令重新生成，旧弱口令 keystore 不再使用"
    - "轮换后主 JAR 与依赖 JAR 仍由同一签名者签名，JCE 受限运算不抛认证异常"
    - "轮换步骤已文档化且不包含任何明文口令"
  artifacts:
    - path: "doc/SIGNING-KEY-ROTATION.md"
      provides: "密钥生成/轮换/配置步骤"
      contains: "keytool"
    - path: "RELEASE.md"
      provides: "轮换章节与 jce-signing 关联"
      contains: "SIGNING-KEY-ROTATION"
  key_links:
    - from: "doc/SIGNING-KEY-ROTATION.md"
      to: "~/.m2/settings.xml"
      via: "jce-signing profile 口令"
      pattern: "jce-signing"
---

<objective>
轮换 JCE 签名密钥库为强口令，并文档化生成/轮换流程，随后验证 JCE 认证仍通过。

Purpose: 消除 CONCERNS C1/C2 的弱口令与不可审计发布问题。
Output: 新 `keystore.jks`（gitignored）、轮换文档、验证证据。
</objective>

<execution_context>
@$HOME/.Codex/get-shit-done/workflows/execute-plan.md
@$HOME/.Codex/get-shit-done/templates/summary.md
@$HOME/.Codex/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-credential-security/02-RESEARCH.md
@.planning/phases/02-credential-security/02-01-PLAN.md
@.planning/phases/02-credential-security/02-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 编写密钥生成/轮换文档</name>
  <files>doc/SIGNING-KEY-ROTATION.md, RELEASE.md</files>
  <read_first>RELEASE.md, pom.xml, .gitignore</read_first>
  <action>新增 `doc/SIGNING-KEY-ROTATION.md`：① 生成命令（`keytool -genkeypair -alias dayou -keyalg RSA -keysize 4096 -sigalg SHA256withRSA -validity 3650 -keystore keystore.jks -storetype JKS -dname "CN=LiuZX JCE Signing, OU=JCE, O=liuzx.org, L=Beijing, ST=Beijing, C=CN"`）；② 强口令要求（≥20 字符、随机、不得复用）；③ 写入 `~/.m2/settings.xml` 的 `jce-signing` profile 示例；④ 轮换步骤（备份旧库→生成新库→更新口令→`mvn clean package` 验证→确认 JCE 认证→弃用旧库）；⑤ 注意事项（`keystore.jks` 与口令绝不入库；轮换后新版本签名者变更，旧已发布产物不受影响）。在 `RELEASE.md` 的 JCE 签名凭据小节链接该文档。文档中不得出现任何真实口令。</action>
  <verify>grep -n "keytool\|jce-signing" doc/SIGNING-KEY-ROTATION.md RELEASE.md</verify>
  <acceptance_criteria>
    - `doc/SIGNING-KEY-ROTATION.md` 含 `keytool`、`jce-signing`、强口令要求
    - 文档中不含任何真实口令字面量
    - `RELEASE.md` 链接到该文档
  </acceptance_criteria>
  <done>轮换流程文档化</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>轮换文档已就绪，需要你生成新密钥库并配置口令（密钥与口令不得由自动化代理持有）。</what-built>
  <how-to-verify>
1. 生成新密钥库（在项目根，会覆盖旧 `keystore.jks`；请先备份旧库）：
   `keytool -genkeypair -alias dayou -keyalg RSA -keysize 4096 -sigalg SHA256withRSA -validity 3650 -keystore keystore.jks -storetype JKS -dname "CN=LiuZX JCE Signing, OU=JCE, O=liuzx.org, L=Beijing, ST=Beijing, C=CN"`
2. 在 `~/.m2/settings.xml` 添加/更新常驻 profile：
   ```xml
   <profile><id>jce-signing</id><properties>
     <jce.storepass>你的强口令</jce.storepass>
     <jce.keypass>你的强口令</jce.keypass>
   </properties></profile>
   ```
3. 回复 "done" 继续（我将执行打包验证）。
  </how-to-verify>
  <resume-signal>Type "done" 或描述问题</resume-signal>
</task>

<task type="auto">
  <name>Task 3: 验证轮换后的签名与 JCE 认证</name>
  <files>pom.xml</files>
  <read_first>pom.xml, scripts/sdf-smoke.sh</read_first>
  <action>执行 `mvn -o clean package -DskipTests`，确认主 JAR 与 `target/lib/jna-5.10.0.jar`、`target/lib/gson-2.9.0.jar` 均由新 alias `dayou` 签名（`jarsigner -verify` 输出 `jar 已验证`）。随后运行一个受限 JCE 运算（`KeyGenerator.getInstance("SM4","LiuZX")` 或 `Cipher.getInstance("SM4","LiuZX")` 的算法解析）以确认不再抛 `JCE cannot authenticate the provider LiuZX`；无设备时仅验证 `KeyGenerator` 的算法解析路径（Provider 注册与认证在 Cipher/KeyGenerator 解析时触发）。无需修改 `pom.xml`，该任务仅验证；若发现问题按 deviation 记录。</action>
  <verify>mvn -o clean package -DskipTests 2>&1 | grep -E "BUILD|jar 已签名" | tail -5</verify>
  <acceptance_criteria>
    - `mvn -o clean package -DskipTests` 退出码 0
    - `jarsigner -verify target/liuzx-sdf-jce-*.jar` 与 `target/lib/jna-5.10.0.jar` 均输出 `jar 已验证`
    - 受限 JCE 运算不抛 `JCE cannot authenticate the provider`
  </acceptance_criteria>
  <done>轮换验证通过</done>
</task>

</tasks>

<verification>
- [ ] 新密钥库口令为强口令且仅存于 settings.xml
- [ ] `mvn -o clean package -DskipTests` 成功
- [ ] 主 JAR 与依赖 JAR 均由同一 alias 签名并通过验证
</verification>

<success_criteria>
- SEC-02 达标：强口令密钥库 + 文档化轮换 + JCE 认证验证
</success_criteria>

<output>
After completion, create `.planning/phases/02-credential-security/02-04-SUMMARY.md`
</output>
