---
phase: 01-api-fa-ade
plan: 04
type: execute
wave: 4
depends_on: ["01-03"]
files_modified:
  - src/test/java/org/liuzx/jce/api/ApiSurfaceAuditTest.java
  - src/main/java/org/liuzx/jce/demo/SdfSmokeTest.java
  - scripts/sdf-smoke.sh
  - doc/SVS-FACADE-1.1.5-CHECKLIST.md
  - pom.xml
autonomous: false
requirements: [API-01, API-12, API-13]
user_setup:
  - service: maven-central
    why: "发布 1.1.5 需要 GPG 口令与 Central User Token"
    env_vars:
      - name: gpg.passphrase
        source: "本机 GPG 私钥口令（或 gpg-agent 缓存）"
    dashboard_config:
      - task: "确认 ~/.m2/settings.xml 已配置 <server id=\"central\"> 与 gpg.keyname/gpg.passphrase"
        location: "本地 ~/.m2/settings.xml"

must_haves:
  truths:
    - "反射审计覆盖 org.liuzx.jce.api 全部公开类型，且零 JNA/Path/File/Pointer/PrivateKey 违规"
    - "至少 KEY_NOT_FOUND、AUTHORIZATION_FAILED、DEVICE_UNAVAILABLE、ALGORITHM_UNSUPPORTED 四类错误可被真机或受控场景触发"
    - "signSm2Digest 与 signRsa 的真机输出可被独立软件验签器用导出的公钥验证通过"
    - "mvn -q verify 通过，且 liuzx-svs 的 DeviceDependencyContractTest 不再报 BLOCKED"
  artifacts:
    - path: "src/test/java/org/liuzx/jce/api/ApiSurfaceAuditTest.java"
      provides: "递归反射审计"
      contains: "org.liuzx.jce.api"
    - path: "doc/SVS-FACADE-1.1.5-CHECKLIST.md"
      provides: "需求实现勾选记录"
      contains: "exportSignPublicKey"
  key_links:
    - from: "scripts/sdf-smoke.sh"
      to: "src/main/java/org/liuzx/jce/demo/SdfSmokeTest.java"
      via: "SMOKE_* 环境变量驱动的真机用例"
      pattern: "SMOKE_SM2_SIGN_INDEX"
---

<objective>
完成门面的最终验收：把反射审计做成覆盖全包并递归解析泛型/数组的硬门禁，扩展冒烟测试覆盖四类错误码与门面签名验签，勾选需求清单，并发布 1.1.5。

Purpose: 证明 API-01/12/13 达标，且 SVS 阻塞真正解除。
Output: 强化的反射审计测试、扩展的冒烟用例、勾选后的清单、发布记录。
</objective>

<execution_context>
@$HOME/.Codex/get-shit-done/workflows/execute-plan.md
@$HOME/.Codex/get-shit-done/templates/summary.md
@$HOME/.Codex/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-api-fa-ade/01-RESEARCH.md
@.planning/phases/01-api-fa-ade/01-VALIDATION.md
@.planning/phases/01-api-fa-ade/01-03-PLAN.md
@doc/SVS-FACADE-1.1.5-CHECKLIST.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 将反射审计升级为递归全包门禁</name>
  <files>src/test/java/org/liuzx/jce/api/ApiSurfaceAuditTest.java</files>
  <read_first>src/test/java/org/liuzx/jce/api/ApiSurfaceAuditTest.java, src/main/java/org/liuzx/jce/api/SdfDevice.java, src/main/java/org/liuzx/jce/api/SdfDevices.java, src/main/java/org/liuzx/jce/api/SdfCapabilities.java</read_first>
  <action>把 `ApiSurfaceAuditTest` 的检查从“直接类型名匹配”升级为递归判定：对 `org.liuzx.jce.api` 下每个 public 类型的每个 public 方法与构造器，递归解析参数与返回类型（展开数组 `Class.getComponentType()`、泛型类型变量 `getActualTypeArguments()`、类型参数上界），只要任一类型的全限定名以 `com.sun.jna`、`org.liuzx.jce.jna` 开头，或等于 `java.nio.file.Path`、`java.io.File`、`java.security.PrivateKey`，或简单名含 `Pointer`，即失败并输出完整签名。同时断言该包至少存在 6 个 public 类型（`SdfErrorCategory`、`SdfException`、`SdfDeviceInfo`、`SdfCapabilities`、`SdfDevice`、`SdfDevices`），并断言 `SdfDeviceInfo` 不存在名为 `deviceSerial`/`getDeviceSerial` 的成员。测试必须能在无硬件条件下运行。</action>
  <verify>mvn -q test -DskipTests=false -Dtest=ApiSurfaceAuditTest</verify>
  <acceptance_criteria>
    - ApiSurfaceAuditTest 递归解析泛型与数组（源码含 `getComponentType` 与 `getActualTypeArguments`）
    - 测试断言 public 类型数 >= 6
    - 测试断言 SdfDeviceInfo 无序列号访问器
    - 在无硬件环境运行不失败
  </acceptance_criteria>
  <done>反射审计成为硬门禁</done>
</task>

<task type="auto">
  <name>Task 2: 扩展冒烟测试覆盖门面签名与四类错误码</name>
  <files>src/main/java/org/liuzx/jce/demo/SdfSmokeTest.java, scripts/sdf-smoke.sh</files>
  <read_first>src/main/java/org/liuzx/jce/demo/SdfSmokeTest.java, scripts/sdf-smoke.sh, src/main/java/org/liuzx/jce/api/SdfDevices.java, src/main/java/org/liuzx/jce/api/SdfDevice.java</read_first>
  <action>在 `SdfSmokeTest` 新增通过 `org.liuzx.jce.api` 门面执行的用例：① `SdfDevices.open()` 后 `exportSignPublicKey(SMOKE_SM2_SIGN_INDEX)` 首字节为 `0x30`；② `signSm2` 长度为 64；③ `signSm2Digest` 长度 64；④ `signRsa` 长度等于模长；⑤ 用不存在索引触发 `KEY_NOT_FOUND`；⑥ 错误 PIN 触发 `AUTHORIZATION_FAILED`；⑦ 未配置设备/断开时触发 `DEVICE_UNAVAILABLE`；⑧ 对不支持算法索引触发 `ALGORITHM_UNSUPPORTED`。逐项打印 `[PASS]/[FAIL]/[SKIP]`，缺失前置条件的用 SKIP。`scripts/sdf-smoke.sh` 新增可选变量 `SMOKE_CHECK_API_FAÇADE=true` 与 `SMOKE_BAD_PIN`、`SMOKE_MISSING_INDEX`，并在摘要中打印门面检查结果。保持既有 14 项真机用例不回归。</action>
  <verify>bash -n scripts/sdf-smoke.sh && mvn -q -DskipTests=true compile</verify>
  <acceptance_criteria>
    - SdfSmokeTest 中新增用例直接 import `org.liuzx.jce.api.SdfDevices`（不出现 `org.liuzx.jce.jna`）
    - sdf-smoke.sh 含 `SMOKE_CHECK_API_FAÇADE` 与 `SMOKE_BAD_PIN` 变量
    - `bash -n scripts/sdf-smoke.sh` 退出码为 0
    - 四类错误码在摘要中各有独立 `[PASS]/[FAIL]/[SKIP]` 行
  </acceptance_criteria>
  <done>冒烟测试覆盖门面与错误分类</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>门面、签名语义与冒烟用例已完成；需要真机确认签名可被独立软件验签器验证，并发布 1.1.5。</what-built>
  <how-to-verify>
1. 在连接真实密码机的主机执行：`SMOKE_VENDOR=Shudun SMOKE_PIN=... SMOKE_SM2_SIGN_INDEX=21 SMOKE_RSA_SIGN_INDEX=11 SMOKE_CHECK_API_FAÇADE=true ./scripts/sdf-smoke.sh`，确认退出码 0 且门面与四类错误码用例无 FAIL。
2. 用导出的公钥与 `signSm2Digest`/`signRsa` 输出，在独立工具（`openssl`/`gmssl` 或纯 Java 验签器）验证通过。
3. 执行 `mvn -q verify` 通过后发布 1.1.5（按 `RELEASE.md`）。
4. 在 `liuzx-svs` 仓库运行 `DeviceDependencyContractTest`，确认不再报 `BLOCKED on liuzx-sdf-jce 1.1.5`。
5. 勾选 `doc/SVS-FACADE-1.1.5-CHECKLIST.md` 中已完成的条目。
  </how-to-verify>
  <resume-signal>Type "approved" 或描述问题</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `mvn -q verify` 通过
- [ ] 反射审计零违规
- [ ] 真机冒烟退出码 0
- [ ] 独立验签器验证通过
- [ ] `liuzx-svs` 契约测试解除 BLOCKED
</verification>

<success_criteria>
- API-01 / API-12 / API-13 达标
- 1.1.5 发布
- SVS 阻塞解除
</success_criteria>

<output>
After completion, create `.planning/phases/01-api-fa-ade/01-04-SUMMARY.md`
</output>
