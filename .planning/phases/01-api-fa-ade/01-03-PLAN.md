---
phase: 01-api-fa-ade
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - src/main/java/org/liuzx/jce/provider/asymmetric/sm2/SM2SignatureSpi.java
  - src/main/java/org/liuzx/jce/api/SdfDeviceImpl.java
  - src/test/java/org/liuzx/jce/api/SdfCapabilitiesTest.java
  - src/test/java/org/liuzx/jce/api/SdfDeviceSignatureSemanticsTest.java
autonomous: true
requirements: [API-05, API-09, API-10, API-11]

must_haves:
  truths:
    - "capabilities().sm2DefaultUserId() 返回实际生效的 UserID，而非消费者猜测的兜底值"
    - "signSm2Digest 直接把入参作为 e 交给设备，不再做 SM3 哈希；长度非 32 时抛 OPERATION_FAILED"
    - "signSm2 输出固定为 64 字节的 r||s，signRsa 输出长度等于模长字节数并保留前导零"
    - "所有签名路径都不缓存 PIN，且 PIN 未出现在异常信息中"
  artifacts:
    - path: "src/main/java/org/liuzx/jce/provider/asymmetric/sm2/SM2SignatureSpi.java"
      provides: "可读取的 SM2 默认 UserID"
      contains: "DEFAULT_USER_ID_STRING"
    - path: "src/main/java/org/liuzx/jce/api/SdfDeviceImpl.java"
      provides: "signSm2 / signSm2Digest / signRsa 实现"
      contains: "signSm2Digest"
  key_links:
    - from: "src/main/java/org/liuzx/jce/api/SdfDeviceImpl.java"
      to: "src/main/java/org/liuzx/jce/provider/util/ASN1Util.java"
      via: "DER ↔ r||s 转换"
      pattern: "fromASN1Signature"
    - from: "src/main/java/org/liuzx/jce/api/SdfDeviceImpl.java"
      to: "src/main/java/org/liuzx/jce/provider/asymmetric/rsa/RSASignatureSpi.java"
      via: "SHA256withRSA JCE 路径"
      pattern: "SDFRSAPrivateKey"
---

<objective>
补齐门面的三个签名方法，并把 SM2 默认 UserID 从硬编码私有常量提升为单一可读取来源。

Purpose: 让 SVS 拿到可被标准软件验签器验证的签名产物，且摘要签名不被二次哈希。
Output: `signSm2`（r||s）、`signSm2Digest`（直接以 e 签名）、`signRsa`（模长输出）+ 语义测试。
</objective>

<execution_context>
@$HOME/.Codex/get-shit-done/workflows/execute-plan.md
@$HOME/.Codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-api-fa-ade/01-RESEARCH.md
@.planning/phases/01-api-fa-ade/01-02-PLAN.md
@doc/SVS-FACADE-1.1.5-CHECKLIST.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 暴露 SM2 默认 UserID 并填充 SdfCapabilities</name>
  <files>src/main/java/org/liuzx/jce/provider/asymmetric/sm2/SM2SignatureSpi.java, src/main/java/org/liuzx/jce/api/SdfDeviceImpl.java, src/test/java/org/liuzx/jce/api/SdfCapabilitiesTest.java</files>
  <read_first>src/main/java/org/liuzx/jce/provider/asymmetric/sm2/SM2SignatureSpi.java, src/main/java/org/liuzx/jce/provider/session/SDFSessionManager.java, src/main/java/org/liuzx/jce/api/SdfCapabilities.java</read_first>
  <action>在 `SM2SignatureSpi` 新增 `public static final String DEFAULT_USER_ID_STRING = "1234567812345678"`，并把现有 `private static final byte[] DEFAULT_USER_ID` 改为由该常量派生（`DEFAULT_USER_ID_STRING.getBytes(StandardCharsets.UTF_8)`），保持签名行为不变。在 `SdfDeviceImpl.capabilities()` 中：`sm2DefaultUserId` 取 `SM2SignatureSpi.DEFAULT_USER_ID_STRING`；`sessionPoolSize`/`sessionPoolAvailable`/`borrowTimeoutMillis` 取 `SDFSessionManager.getInstance()` 的对应 getter；`families` 依据 `deviceInfo()` 的 `symAlgAbility`/`hashAlgAbility` 与能力位判定，至少包含 SM2 与 SM3，RSA 依据设备信息判定（无法判定时保守包含 RSA，并在 JavaDoc 说明）。`sm2DigestSigningSupported` 为 true。`SdfCapabilitiesTest` 断言 `sm2DefaultUserId()` 等于 `SM2SignatureSpi.DEFAULT_USER_ID_STRING` 且非空，并断言集合不可变（修改抛 UnsupportedOperationException）。无设备时该断言仍可运行（不依赖打开设备的部分用包私有工厂方法）。</action>
  <verify>mvn -q test -DskipTests=false -Dtest=SdfCapabilitiesTest</verify>
  <acceptance_criteria>
    - SM2SignatureSpi.java 含 `public static final String DEFAULT_USER_ID_STRING = "1234567812345678"`
    - SM2SignatureSpi.java 中 DEFAULT_USER_ID 由 DEFAULT_USER_ID_STRING 派生（不再有独立字面量）
    - SdfCapabilitiesTest 断言 families() 返回不可变集合
  </acceptance_criteria>
  <done>UserID 单一来源且进入能力指纹</done>
</task>

<task type="auto">
  <name>Task 2: 实现 signSm2 与 signSm2Digest</name>
  <files>src/main/java/org/liuzx/jce/api/SdfDeviceImpl.java, src/test/java/org/liuzx/jce/api/SdfDeviceSignatureSemanticsTest.java</files>
  <read_first>src/main/java/org/liuzx/jce/provider/asymmetric/sm2/SM2SignatureSpi.java, src/main/java/org/liuzx/jce/provider/asymmetric/sm2/SM2PrivateKey.java, src/main/java/org/liuzx/jce/provider/util/ASN1Util.java, src/main/java/org/liuzx/jce/provider/session/SDFSessionManager.java</read_first>
  <action>`signSm2(keyIndex, message, pinOrNull)`：借用会话，`SDF_ExportSignPublicKey_ECC` 导出公钥构造 `SM2PrivateKey(keyIndex, pinOrNull, eccPublicKey)`，用 `Signature.getInstance("SM3withSM2", "LiuZX")` + `initSign` + `update(message)` 得到 ASN.1 DER 签名，再用 `ASN1Util.fromASN1Signature(der)` 还原 `r[32]`/`s[32]` 并返回 64 字节 `r||s`。`signSm2Digest(keyIndex, digest, pinOrNull)`：若 `digest == null || digest.length != 32` 抛 `new SdfException(OPERATION_FAILED, "signSm2Digest", 0, null)`；否则借用会话，导出公钥，按需 `getPrivateKeyAccessRight`，直接调用 `SDF_InternalSign_ECC(session.getSessionHandle(), keyIndex, digest, 32, eccSignature)`（**不得**调用 `SDF_HashInit/Update/Final`，**不得**再哈希），finally 释放私钥访问权，返回 `r||s`。两次失败都经 `SdfErrorMapper.map(rv)` 转成 `SdfException`；异常信息不得含 PIN。`SdfDeviceSignatureSemanticsTest`：无设备时用 `Assumptions` 跳过真机部分，但必须包含纯逻辑断言——构造 31/33 字节输入断言抛 `SdfException` 且 `category() == OPERATION_FAILED`（可通过包私有静态校验方法 `validateDigestLength(byte[])` 在无设备下测试）。</action>
  <verify>mvn -q test -DskipTests=false -Dtest=SdfDeviceSignatureSemanticsTest</verify>
  <acceptance_criteria>
    - SdfDeviceImpl.java 的 signSm2Digest 方法体不出现 `SDF_HashInit`/`SDF_HashUpdate`/`SDF_HashFinal`/`MessageDigest`
    - signSm2 返回长度恒为 64（`r.length + s.length == 64`）
    - `validateDigestLength(null)`、`validateDigestLength(new byte[31])` 均抛 OPERATION_FAILED
    - SdfDeviceImpl.java 无 `char[]` 字段
  </acceptance_criteria>
  <done>SM2 原文签名与摘要签名语义正确</done>
</task>

<task type="auto">
  <name>Task 3: 实现 signRsa（SHA-256 / PKCS#1 v1.5 / 模长输出）</name>
  <files>src/main/java/org/liuzx/jce/api/SdfDeviceImpl.java</files>
  <read_first>src/main/java/org/liuzx/jce/provider/asymmetric/rsa/RSASignatureSpi.java, src/main/java/org/liuzx/jce/provider/asymmetric/rsa/SDFRSAPrivateKey.java, src/main/java/org/liuzx/jce/provider/asymmetric/rsa/RSAKeyConverter.java</read_first>
  <action>`signRsa(keyIndex, message, pinOrNull)`：借用会话，`SDF_ExportSignPublicKey_RSA` 经 `RSAKeyConverter` 得到标准 `RSAPublicKey`，构造 `SDFRSAPrivateKey(keyIndex, pinOrNull, rsaPublicKey)`，用 `Signature.getInstance("SHA256withRSA", "LiuZX")` 完成签名（该路径已在 `RSASignatureSpi` 中实现 EMSA-PKCS1 v1.5 与内部密钥裸运算，输出为模长字节）。返回前断言输出长度等于 `rsaPublicKey.getModulus().bitLength() / 8`，不等则抛 `SdfException(OPERATION_FAILED, "signRsa", 0, null)`。会话在 finally 归还；不得缓存 PIN 或公钥。</action>
  <verify>mvn -q test -DskipTests=false -Dtest=SdfDeviceSignatureSemanticsTest</verify>
  <acceptance_criteria>
    - signRsa 使用 `SHA256withRSA` 与 `SDFRSAPrivateKey`，不新写 PKCS#1 编码
    - signRsa 返回长度等于模长字节数（2048→256，4096→512）
    - SdfDeviceImpl.java 不出现 `SDF_InternalPrivateKeyOperation_RSA` 的直接调用
  </acceptance_criteria>
  <done>RSA 签名输出满足模长与验证互通要求</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `mvn -q test -DskipTests=false -Dtest='org.liuzx.jce.api.**'` 通过
- [ ] 无设备时可跳过的测试不失败
- [ ] SM2 SPI 签名行为未变（既有 `SM2SignatureTest` 在有设备时仍通过）
</verification>

<success_criteria>
- signSm2 / signSm2Digest / signRsa 三个方法实现且语义符合规格
- sm2DefaultUserId 来自单一来源
- PIN 不缓存、不泄漏
</success_criteria>

<output>
After completion, create `.planning/phases/01-api-fa-ade/01-03-SUMMARY.md`
</output>
