---
phase: 01-api-fa-ade
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/main/java/org/liuzx/jce/api/SdfDevice.java
  - src/main/java/org/liuzx/jce/api/SdfDevices.java
  - src/main/java/org/liuzx/jce/api/SdfDeviceImpl.java
  - src/test/java/org/liuzx/jce/api/SdfDeviceLifecycleTest.java
  - src/test/java/org/liuzx/jce/api/SdfDeviceExportKeyTest.java
autonomous: true
requirements: [API-06, API-07, API-08, API-11]

must_haves:
  truths:
    - "消费方可通过 SdfDevices.open() 获得 SdfDevice，且签名中不出现 SDFLibrary、Pointer 或会话句柄"
    - "exportSignPublicKey(int) 返回的字节可被标准 X.509 解析器解析为 SubjectPublicKeyInfo"
    - "close() 幂等，且关闭后调用任何方法抛 IllegalStateException"
    - "PIN 只在单次调用内存活，门面不保存到字段或缓存"
  artifacts:
    - path: "src/main/java/org/liuzx/jce/api/SdfDevice.java"
      provides: "消费方门面接口"
      contains: "interface SdfDevice"
    - path: "src/main/java/org/liuzx/jce/api/SdfDevices.java"
      provides: "入口工厂"
      contains: "class SdfDevices"
    - path: "src/main/java/org/liuzx/jce/api/SdfDeviceImpl.java"
      provides: "包私有的门面实现"
      contains: "class SdfDeviceImpl"
  key_links:
    - from: "src/main/java/org/liuzx/jce/api/SdfDevices.java"
      to: "src/main/java/org/liuzx/jce/provider/session/SDFSessionManager.java"
      via: "getInstance() 取得会话池"
      pattern: "SDFSessionManager"
    - from: "src/main/java/org/liuzx/jce/api/SdfDeviceImpl.java"
      to: "src/main/java/org/liuzx/jce/jna/SDFLibrary.java"
      via: "SDF_ExportSignPublicKey_ECC / _RSA"
      pattern: "SDF_ExportSignPublicKey"
---

<objective>
交付 `SdfDevice` 门面接口、`SdfDevices` 入口工厂与包私有实现，覆盖设备信息、能力指纹、内部公钥导出与生命周期，且不向调用方泄漏任何 JNA 类型。

Purpose: 把 SVS 需要的“不触碰 JNA 即可签名”能力落到接口与工厂上。
Output: `SdfDevice`、`SdfDevices`、`SdfDeviceImpl` + 生命周期与公钥导出测试。
</objective>

<execution_context>
@$HOME/.Codex/get-shit-done/workflows/execute-plan.md
@$HOME/.Codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-api-fa-ade/01-RESEARCH.md
@.planning/phases/01-api-fa-ade/01-VALIDATION.md
@.planning/phases/01-api-fa-ade/01-01-PLAN.md
@doc/SVS-FACADE-1.1.5-CHECKLIST.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 定义 SdfDevice 接口与 SdfDevices 入口工厂</name>
  <files>src/main/java/org/liuzx/jce/api/SdfDevice.java, src/main/java/org/liuzx/jce/api/SdfDevices.java, src/main/java/org/liuzx/jce/api/SdfDeviceImpl.java, src/test/java/org/liuzx/jce/api/SdfDeviceLifecycleTest.java</files>
  <read_first>src/main/java/org/liuzx/jce/provider/session/SDFSessionManager.java, src/main/java/org/liuzx/jce/provider/util/DeviceInfoUtil.java, src/main/java/org/liuzx/jce/api/SdfCapabilities.java, src/main/java/org/liuzx/jce/api/SdfDeviceInfo.java, .planning/phases/01-api-fa-ade/01-RESEARCH.md</read_first>
  <action>`public interface SdfDevice extends AutoCloseable`，方法严格为 `SdfDeviceInfo deviceInfo()`、`SdfCapabilities capabilities()`、`byte[] exportSignPublicKey(int keyIndex)`、`byte[] signSm2(int keyIndex, byte[] message, char[] pinOrNull)`、`byte[] signSm2Digest(int keyIndex, byte[] digest, char[] pinOrNull)`、`byte[] signRsa(int keyIndex, byte[] message, char[] pinOrNull)`、`void close()`。`public final class SdfDevices` 提供 `static SdfDevice open()` 与 `static SdfDevice open(java.util.Properties overrides)`；`open(Properties)` 只接受非凭据的白名单键（至少支持透传 `liuzx.sdf.vendor`、`liuzx.sdf.library.path`、`liuzx.sdf.profile.path`、`liuzx.sdf.vendor-config.path`），遇未知键抛 `IllegalArgumentException`，遇含 `passwd`/`password`/`pin` 的键抛 `IllegalArgumentException`。工厂内部经 `SDFSessionManager.getInstance()` 预热并返回包私有 `SdfDeviceImpl`。`SdfDeviceImpl` 持有 `volatile boolean closed`，`close()` 用 CAS 保证幂等并释放借用；所有公开方法首行 `ensureOpen()`，关闭后抛 `IllegalStateException("SdfDevice is closed")`。`deviceInfo()` 将 `DeviceInfoUtil.DeviceInfo` 投影为 `SdfDeviceInfo`，`deviceModelClass` 用 `deviceName` 的型号分类且**不得**透出 `getDeviceSerial()`；`libraryDigestPrefix` 使用已加载库摘要前缀（无摘要时用固定占位 `"unknown"`）。本任务中 sign* 方法先抛 `UnsupportedOperationException("implemented in plan 01-03")`。`SdfDeviceLifecycleTest` 断言 `close()` 两次不抛异常、关闭后 `deviceInfo()`/`capabilities()` 抛 `IllegalStateException`（该测试需要设备；若无法打开则用 `Assumptions.assumeTrue` 跳过，并把纯逻辑的 closed 状态机抽成包私有可测方法）。</action>
  <verify>mvn -q test -DskipTests=false -Dtest=SdfDeviceLifecycleTest</verify>
  <acceptance_criteria>
    - SdfDevice.java 的 import 与签名中不出现 `com.sun.jna`、`org.liuzx.jce.jna`、`Pointer`、`SDFLibrary`
    - SdfDevices.java 的公开方法只有 `open()` 与 `open(Properties)`
    - SdfDevices.open(Properties) 对含 `pin`/`password` 的键抛 IllegalArgumentException
    - SdfDeviceImpl 中不存在类型为 `char[]` 的字段
    - SdfDeviceLifecycleTest 通过或被 assumeTrue 跳过且不失败
  </acceptance_criteria>
  <done>门面接口与工厂可用，生命周期状态机可测</done>
</task>

<task type="auto">
  <name>Task 2: 实现内部公钥导出为 X.509</name>
  <files>src/main/java/org/liuzx/jce/api/SdfDeviceImpl.java, src/test/java/org/liuzx/jce/api/SdfDeviceExportKeyTest.java</files>
  <read_first>src/main/java/org/liuzx/jce/provider/asymmetric/sm2/SM2KeyPairGenerator.java, src/main/java/org/liuzx/jce/provider/asymmetric/sm2/SM2PublicKey.java, src/main/java/org/liuzx/jce/provider/asymmetric/rsa/RSAKeyPairGeneratorSpi.java, src/main/java/org/liuzx/jce/provider/asymmetric/rsa/RSAKeyConverter.java, src/main/java/org/liuzx/jce/provider/util/ASN1Util.java</read_first>
  <action>在 `SdfDeviceImpl.exportSignPublicKey(int)` 中借用会话并先调用 `SDF_ExportSignPublicKey_ECC`（SM2）构造 `SM2PublicKey` 并返回其 `getEncoded()`（X.509 SubjectPublicKeyInfo）；若 ECC 导出返回非零，再调用 `SDF_ExportSignPublicKey_RSA`，经 `RSAKeyConverter` 得到标准 `RSAPublicKey` 并用 `KeyFactory.getInstance("RSA").generatePublic(new X509EncodedKeySpec(...))` 编码。两次都失败时抛出映射后的 `SdfException`（分类用 `SdfErrorMapper.map(lastCode)`）。会话必须在 `finally` 中归还；不得把设备公钥缓存到字段（不满足“不得长期缓存公钥”之外的额外约束，但保持每次导出）。同步在 `SdfDevice.java` 的 JavaDoc 注明“先 SM2 后 RSA 探测”的行为。`SdfDeviceExportKeyTest` 需要真实设备时用 `Assumptions` 跳过；无设备时可测的部分：断言导出的非空字节可被 `java.security.KeyFactory` 或 `sun.security.x509` 之外的通用解析路径解析——用 `X509EncodedKeySpec` + `KeyFactory("EC"/"RSA")` 尝试，至少断言前导字节符合 DER SEQUENCE（`0x30`）。</action>
  <verify>mvn -q test -DskipTests=false -Dtest=SdfDeviceExportKeyTest</verify>
  <acceptance_criteria>
    - exportSignPublicKey 返回值非空时首字节为 `0x30`（DER SEQUENCE）
    - SdfDeviceImpl 中不出现 `char[]` 字段
    - 会话在 finally 中归还（源码含 `finally` 且调用 session 归还/关闭）
    - 导出的 RSA 路径使用 `RSAKeyConverter` 而非新写布局转换
  </acceptance_criteria>
  <done>内部公钥可经门面导出为 X.509</done>
</task>

<task type="auto">
  <name>Task 3: 落实 PIN 按需申请与释放语义</name>
  <files>src/main/java/org/liuzx/jce/api/SdfDeviceImpl.java, src/main/java/org/liuzx/jce/api/SdfDevice.java</files>
  <read_first>src/main/java/org/liuzx/jce/provider/session/SDFSessionManager.java, src/main/java/org/liuzx/jce/provider/symmetric/SDFInternalKeyHandleResolver.java, .planning/phases/01-api-fa-ade/01-RESEARCH.md</read_first>
  <action>在 `SdfDeviceImpl` 增加包私有方法 `private static int withPrivateKeyAccess(int keyIndex, char[] pinOrNull, PrivateKeyAction action)` 形状的执行辅助：`pinOrNull == null || pinOrNull.length == 0` 时不调用 `SDF_GetPrivateKeyAccessRight`；否则先 `getPrivateKeyAccessRight(session, keyIndex, pinOrNull)`，在 `finally` 中调用 `SDF_ReleasePrivateKeyAccessRight`。禁止把 `pinOrNull` 写入任何字段、静态缓存或日志；异常信息不得包含 PIN。`SdfDevice` 的对应方法 JavaDoc 注明“PIN 由调用方负责清零，门面不缓存”。本任务不实现实际签名算法（留待 01-03），但辅助函数与测试需就绪。</action>
  <verify>mvn -q test -DskipTests=false -Dtest=SdfDeviceLifecycleTest</verify>
  <acceptance_criteria>
    - SdfDeviceImpl.java 不含 `static.*char\[\]` 缓存字段
    - `withPrivateKeyAccess` 的 finally 中包含释放动作
    - SdfDevice.java 的 signSm2/signSm2Digest/signRsa JavaDoc 含“不缓存”字样
  </acceptance_criteria>
  <done>PIN 生命周期受控，无字段缓存</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `mvn -q test -DskipTests=false -Dtest='org.liuzx.jce.api.**'` 通过
- [ ] 反射审计仍零违规（ApiSurfaceAuditTest）
- [ ] `SdfDevices.open()` 的签名不含 JNA 类型
</verification>

<success_criteria>
- `SdfDevice`/`SdfDevices`/`SdfDeviceImpl` 存在且不泄漏 JNA
- 内部公钥可导出为 X.509
- PIN 按需申请、finally 释放、无字段缓存
- close() 幂等，关闭后抛 IllegalStateException
</success_criteria>

<output>
After completion, create `.planning/phases/01-api-fa-ade/01-02-SUMMARY.md`
</output>
