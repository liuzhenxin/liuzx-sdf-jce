---
phase: 01-api-fa-ade
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/java/org/liuzx/jce/api/SdfErrorCategory.java
  - src/main/java/org/liuzx/jce/api/SdfErrorMapper.java
  - src/main/java/org/liuzx/jce/api/SdfException.java
  - src/main/java/org/liuzx/jce/api/SdfDeviceInfo.java
  - src/main/java/org/liuzx/jce/api/SdfCapabilities.java
  - src/test/java/org/liuzx/jce/api/ApiSurfaceAuditTest.java
  - src/test/java/org/liuzx/jce/api/SdfErrorCategoryMappingTest.java
  - src/test/java/org/liuzx/jce/api/SdfDeviceInfoSafeStringTest.java
autonomous: true
requirements: [API-01, API-02, API-03, API-04, API-05]

must_haves:
  truths:
    - "org.liuzx.jce.api 的公开类型只引用 JDK 类型，不出现 com.sun.jna / org.liuzx.jce.jna / java.nio.file.Path / java.io.File"
    - "SdfException 的 internalDetail 只包含操作名与十六进制厂商码，不含路径、PIN 或密钥索引"
    - "SdfDeviceInfo 没有任何返回序列号的访问器，toSafeString() 与 toString() 都不含序列号"
  artifacts:
    - path: "src/main/java/org/liuzx/jce/api/SdfErrorCategory.java"
      provides: "九类稳定错误分类枚举"
      contains: "enum SdfErrorCategory"
    - path: "src/main/java/org/liuzx/jce/api/SdfException.java"
      provides: "公开异常，携带分类与脱敏细节"
      contains: "class SdfException"
    - path: "src/main/java/org/liuzx/jce/api/SdfDeviceInfo.java"
      provides: "脱敏设备信息 record"
      contains: "record SdfDeviceInfo"
    - path: "src/main/java/org/liuzx/jce/api/SdfCapabilities.java"
      provides: "能力指纹 record"
      contains: "record SdfCapabilities"
  key_links:
    - from: "src/main/java/org/liuzx/jce/api/SdfException.java"
      to: "src/main/java/org/liuzx/jce/api/SdfErrorCategory.java"
      via: "category() 返回枚举，构造经 SdfErrorMapper"
      pattern: "SdfErrorCategory"
---

<objective>
建立 `org.liuzx.jce.api` 稳定公开包，交付四个不泄漏 JNA 的公开类型（`SdfErrorCategory`、`SdfException`、`SdfDeviceInfo`、`SdfCapabilities`）与反射审计测试骨架。

Purpose: 这是 SVS 门面的类型基础；后续计划的门面与工厂都依赖这些类型。
Output: 4 个公开类型 + 1 个包内错误映射器 + 3 个测试骨架。
</objective>

<execution_context>
@$HOME/.Codex/get-shit-done/workflows/execute-plan.md
@$HOME/.Codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-api-fa-ade/01-RESEARCH.md
@.planning/phases/01-api-fa-ade/01-VALIDATION.md
@doc/SVS-FACADE-1.1.5-CHECKLIST.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建公开错误分类枚举与映射器</name>
  <files>src/main/java/org/liuzx/jce/api/SdfErrorCategory.java, src/main/java/org/liuzx/jce/api/SdfErrorMapper.java, src/test/java/org/liuzx/jce/api/SdfErrorCategoryMappingTest.java</files>
  <read_first>src/main/java/org/liuzx/jce/provider/exception/SDFErrorConstants.java, src/main/java/org/liuzx/jce/provider/exception/SDFException.java, .planning/phases/01-api-fa-ade/01-RESEARCH.md</read_first>
  <action>在 `org.liuzx.jce.api` 下创建 `public enum SdfErrorCategory`，常量严格为九项：DEVICE_UNAVAILABLE, DEVICE_BUSY, KEY_NOT_FOUND, KEY_USAGE_MISMATCH, ALGORITHM_UNSUPPORTED, AUTHORIZATION_FAILED, OPERATION_FAILED, INPUT_TOO_LARGE, NATIVE_DEPENDENCY_UNAVAILABLE。创建包私有 `final class SdfErrorMapper`，提供 `static SdfErrorCategory map(int sdfCode)` 与 `static boolean isRetryable(SdfErrorCategory category)`，映射表按 RESEARCH §5：SDR_COMMFAIL(0x01000003)→DEVICE_UNAVAILABLE、SDR_HSM_NOT_READY(0x01000403)→DEVICE_BUSY、SDR_KEYNOTEXIST(0x01000008)→KEY_NOT_FOUND、SDR_KEYTYPEERR/SDR_KEYERR→KEY_USAGE_MISMATCH、SDR_ALGNOTSUPPORT/SDR_ALGMODNOTSUPPORT→ALGORITHM_UNSUPPORTED、SDR_PARDENY/SDR_PRKRERR→AUTHORIZATION_FAILED、SDR_INARGERR/SDR_OUTARGERR→INPUT_TOO_LARGE、其余→OPERATION_FAILED；isRetryable 仅 DEVICE_UNAVAILABLE 与 DEVICE_BUSY 为 true。测试 `SdfErrorCategoryMappingTest` 逐一断言上述映射，并断言枚举恰好 9 个常量。注意 SDFErrorConstants 是接口常量，不要新增对 JNA 的引用。</action>
  <verify>mvn -q test -DskipTests=false -Dtest=SdfErrorCategoryMappingTest</verify>
  <acceptance_criteria>
    - SdfErrorCategory.java 恰好包含 9 个枚举常量
    - SdfErrorCategoryMappingTest 对 SDR_COMMFAIL/SDR_HSM_NOT_READY/SDR_KEYNOTEXIST/SDR_ALGNOTSUPPORT/SDR_PARDENY/SDR_INARGERR 的断言全部通过
    - `map(SDR_OK)` 不被调用或抛出 IllegalArgumentException（SDR_OK 不是错误）
  </acceptance_criteria>
  <done>九类枚举与映射器可用，测试通过</done>
</task>

<task type="auto">
  <name>Task 2: 实现脱敏的公开异常 SdfException</name>
  <files>src/main/java/org/liuzx/jce/api/SdfException.java</files>
  <read_first>src/main/java/org/liuzx/jce/provider/exception/SDFException.java, src/main/java/org/liuzx/jce/api/SdfErrorMapper.java, .planning/phases/01-api-fa-ade/01-RESEARCH.md</read_first>
  <action>`public class SdfException extends RuntimeException`，构造器包私有 `SdfException(SdfErrorCategory category, String operation, int sdfCode, Throwable cause)`。公开方法 `category()`、`operation()`、`internalDetail()`、`isRetryable()`。`internalDetail()` 只返回 `operation + " (0x" + 8位大写十六进制 + ")"`，禁止拼接路径、PIN、密钥索引或设备序列号；`getMessage()` 使用 `operation + " failed"`。`isRetryable()` 委托 `SdfErrorMapper.isRetryable(category)`。不得 import `org.liuzx.jce.jna..`、`com.sun.jna..`、`java.nio.file..`、`java.io.File`。</action>
  <verify>mvn -q test -DskipTests=false -Dtest=ApiSurfaceAuditTest</verify>
  <acceptance_criteria>
    - SdfException.java 的 import 中不出现 `com.sun.jna`、`org.liuzx.jce.jna`、`java.nio.file`、`java.io.File`
    - SdfException.java 的公开方法签名不含 `Pointer`、`Path`、`File`、`RSAPublicKey`、`PrivateKey`
    - `internalDetail()` 输出形如 `signSm2 (0x01000008)`，不含 `/`、`\\`、`.so`、`.ini`
  </acceptance_criteria>
  <done>公开异常可携带分类与脱敏细节，反射审计通过</done>
</task>

<task type="auto">
  <name>Task 3: 定义脱敏设备信息与能力指纹 record</name>
  <files>src/main/java/org/liuzx/jce/api/SdfDeviceInfo.java, src/main/java/org/liuzx/jce/api/SdfCapabilities.java, src/test/java/org/liuzx/jce/api/SdfDeviceInfoSafeStringTest.java, src/test/java/org/liuzx/jce/api/ApiSurfaceAuditTest.java</files>
  <read_first>src/main/java/org/liuzx/jce/provider/util/DeviceInfoUtil.java, src/main/java/org/liuzx/jce/provider/asymmetric/sm2/SM2SignatureSpi.java, src/main/java/org/liuzx/jce/provider/session/SDFSessionManager.java, .planning/phases/01-api-fa-ade/01-RESEARCH.md</read_first>
  <action>创建 `public record SdfDeviceInfo(String issuerName, String deviceName, String deviceModelClass, int deviceVersion, int standardVersion, int symAlgAbility, int hashAlgAbility, int bufferSize, String libraryDigestPrefix)`，只提供 `toSafeString()`（显式列出各字段但不含序列号、路径、库文件名），**不得**提供 `deviceSerial()`/`getDeviceSerial()` 访问器；`libraryDigestPrefix` 仅是摘要前缀。创建 `public record SdfCapabilities(Set<AlgorithmFamily> families, boolean sm2DigestSigningSupported, String sm2DefaultUserId, int sessionPoolSize, int sessionPoolAvailable, long borrowTimeoutMillis)`；在同一包新增 `public enum AlgorithmFamily { SM2, SM3, RSA }`；集合在构造器中做不可变拷贝（`Set.copyOf`）。`ApiSurfaceAuditTest` 用反射遍历 `org.liuzx.jce.api` 下所有公开类型的公开方法与构造器，断言参数/返回类型的完整名称不包含 `com.sun.jna`、`org.liuzx.jce.jna`、`java.nio.file.`、`java.io.File`、`Pointer`、`PrivateKey`。`SdfDeviceInfoSafeStringTest` 构造一个含敏感序列号的实例，断言 `toSafeString()` 与 `toString()` 都不包含该序列号。</action>
  <verify>mvn -q test -DskipTests=false -Dtest='ApiSurfaceAuditTest+SdfDeviceInfoSafeStringTest'</verify>
  <acceptance_criteria>
    - SdfDeviceInfo.java 中不存在名为 `deviceSerial` 或 `getDeviceSerial` 的成员
    - SdfDeviceInfoSafeStringTest 断言 `toSafeString()` 不含注入的序列号字符串且通过
    - ApiSurfaceAuditTest 扫描到至少 5 个公开类型且零违规
    - SdfCapabilities.java 含字段 `sm2DefaultUserId`、`sessionPoolSize`、`sessionPoolAvailable`、`borrowTimeoutMillis`
  </acceptance_criteria>
  <done>两个 record 与方法集合不可变，脱敏与反射审计测试通过</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `mvn -q test -DskipTests=false -Dtest='org.liuzx.jce.api.**'` 全部通过
- [ ] `mvn -q compile` 无错误
- [ ] 反射审计确认 `org.liuzx.jce.api` 公开签名无 JNA / Path / File / Pointer / PrivateKey
</verification>

<success_criteria>
- 4 个公开类型存在于 `org.liuzx.jce.api`
- 九类错误分类与映射器可用
- 脱敏与反射审计测试通过
- 不修改任何现有 Provider 内部实现
</success_criteria>

<output>
After completion, create `.planning/phases/01-api-fa-ade/01-01-SUMMARY.md`
</output>
